{
  "name": "ajaywedsaishu",
  "version": "1.0.0",
  "description": "TITLE:  Wedding - 100% Fully Responsive Free HTML5 Bootstrap Template",
  "main": "index.html",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rm -rfv build/* && cp -R css build && cp -R fonts build && cp -R images build && cp -R js build && cp index.html build"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "serve": "^14.0.1"
  }
}
